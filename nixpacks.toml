[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["cp -r frontend backend/frontend || true"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
